@model PagedList.IPagedList<DelmoChickenWebApp.Models.Product>
@{
    ViewBag.Title = "ProductList";
}

<h2>Our Products</h2>

@*<div class="form-group" >
        <form method="get" action="@Url.Action("ProductList")" data-otf-ajax="true" data-otf-target="#restaurantList">
            <table align="center">
                <tr>
                    <td>
                        <input class="form-control" type="search" placeholder="Product..." name="searchProduct" data-otf-autocomplete="@Url.Action("Autocomplete")" />
                    </td>
                    <td>
                        <input class=" glyphicon-edit btn btn-success" type="submit" value="Search By Name" />
                    </td>
                </tr>
            </table>
        </form>
    </div>*@

@*<form class="navbar-form" role="search">
        <div class="input-group">
            <table align="center">
                <tr>
                    <td>
                        <input class="form-control" type="search" placeholder="Product..." name="SearchString" data-otf-autocomplete="@Url.Action("Autocomplete")" />
                    </td>
                    <td>
                        <input class="glyphicon-edit btn btn-success" type="submit" value="Search By Name" />
                    </td>
                </tr>
            </table>
        </div>
    </form>*@

<div class="form-group">
    <form role="search">
        <table align="center">
            <tr>
                <td>
                    <input class="form-control" type="search" placeholder="Product..." name="SearchString" data-otf-autocomplete="@Url.Action("Autocomplete")" />
                </td>
                <td>
                    <button class="btn btn-success" type="submit" value=""> <i class="glyphicon glyphicon-search"></i></button>
                </td>
            </tr>
        </table>
    </form>
</div>


@*<form method="get" action="@Url.Action("ProductList")"
          data-otf-ajax="true" data-otf-target="#restaurantList">

        <input type="search" name="searchTerm" data-otf-autocomplete="@Url.Action("Autocomplete")" />
        <input type="submit" value="Search By Name" />
    </form>*@
@Html.Partial("_Products", Model)
